<div class="row justify-content-center">
  <div class="col col-md-6">
    <h1 class="text-center"><%= t('.telephone_book') %> : <%= @telephone_book.title %></h1>
    <%= link_to t('.new'), new_telephone_book_contact_path(@telephone_book), class: 'btn btn-success' %>
    <%= link_to t('.back'), telephone_books_path, class: 'btn btn-primary'%>
    <%= render 'import_form' %>
    <table class="table table-hover my-3">
      <thead class="thead-dark">
        <tr class="text-center">
          <th><%= t('.name') %></th>
          <th><%= t('.phone') %></th>
          <th colspan="4"><%= t('.actions') %></th>
        </tr>
      </thead>
    <%= render @telephone_book.contacts%>
    </table>


  </div>
</div>
